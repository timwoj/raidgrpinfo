runtime: python312
app_engine_apis: true

handlers:
- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico

- url: /robots\.txt
  static_files: robots.txt
  upload: robots.txt

- url: /static
  static_dir: static

- url: .*
  script: main.app
